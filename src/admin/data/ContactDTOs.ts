export type AddConctactDTO = {
  email: string;
  message: string;
  name: string;
};

export type ContactDTO = {
  createdAt: Date;
  email: string;
  id: string;
  message: string;
  name: string;
};
